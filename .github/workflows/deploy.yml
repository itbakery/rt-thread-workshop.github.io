name: Deploy mdBook to GitHub Pages

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

# Sets permissions for GITHUB_TOKEN to enable deployment
permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Fetch all history for .gitmodules

    - name: Setup mdBook
      uses: peaceiris/actions-mdbook@v1
      with:
        mdbook-version: '0.4.36'

    - name: Build mdBook
      run: mdbook build

    - name: Setup Pages
      uses: actions/configure-pages@v5   # ⬅️ updated from v3 → v5
      with:
        enablement: true

    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3   # ⬅️ updated from v2 → v3
      with:
        path: ./book

    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4   # ⬅️ updated from v2 → v4
